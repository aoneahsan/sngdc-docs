nodeLinker: node-modules

enableGlobalCache: true

# Use the traditional node_modules layout instead of Plug'n'Play.
# Reason: Docusaurus 3.10 + recent Node hits ERR_INTERNAL_ASSERTION inside the
# PnP loader — the node-modules linker sidesteps it cleanly. Matches the proven
# native-update-docs / ztools-docs setup.
